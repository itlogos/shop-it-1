spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/catalogue
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver
  flyway:
    schemas:
      - public
      - catalogue
    enabled: true
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
server:
  port: 8081
logging:
  level:
    sql: info
